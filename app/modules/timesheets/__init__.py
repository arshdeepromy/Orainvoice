"""Staff timesheets module — clock-to-payslip aggregation, approval, and settings.

Phases:
  A — Timesheet entity, aggregation, match engine, settings, branch scoping.
  B — Pay cycles, auto-period generation, locked-to-payslip, corrections.
  C — Overtime auto-detect, break enforcement, public holidays, leave engine.
  D — PAYE engine (stub), IRD filing (stub), bank export (stub).
"""
