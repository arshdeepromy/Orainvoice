"""E-Signature Integration module (`esignatures`).

Lets authorised organisation users send PDFs for legally-binding digital
signature through a self-hosted Documenso instance, track signing progress,
and store the completed signed PDF back inside OraInvoice.

Documenso is the signing engine only; OraInvoice remains the system of record.
"""
