/**
 * Push Notifications — convenience re-export.
 *
 * Import from `@/native` instead of `@/hooks` for consistency.
 */

export { usePushNotifications } from '@/hooks/usePushNotifications'
export type { UsePushNotificationsResult } from '@/hooks/usePushNotifications'
