"""Test package for the B2B Fleet Portal feature.

All property-based tests, unit tests, integration tests, and smoke tests
for ``app.modules.fleet_portal`` live under this directory. The spec
file (``.kiro/specs/b2b-fleet-portal/tasks.md``) lists the file-to-task
mapping; checkpoints run scoped subsets of these tests rather than the
full project suite.
"""
