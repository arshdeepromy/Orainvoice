/**
 * Job Cards page barrel (Task 27).
 *
 * Mirrors frontend/src/pages/job-cards. Routed under /job-cards, gated by the
 * `jobs` module (matching the original router).
 */
export { default as JobCardCreate } from './JobCardCreate'
export { default as JobCardDetail } from './JobCardDetail'
export { default as JobCardList } from './JobCardList'
